# Copy this content to your .env file
# Property Management Backend Environment Configuration

APP_NAME="Property Management Backend"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration - PostgreSQL 17
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=property_management
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# Mail Configuration - Gmail SMTP
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=sv1829500@gmail.com
MAIL_PASSWORD=mijobgbtqgyifqpw
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=sv1829500@gmail.com
MAIL_FROM_NAME="${APP_NAME}"
MAIL_MAILER_DRIVER=smtp

# Queue Configuration
QUEUE_CONNECTION=database
QUEUE_MONITORING_ENABLED=true
QUEUE_MONITORING_INTERVAL=60
QUEUE_ALERT_THRESHOLD=100
QUEUE_STALE_THRESHOLD=300

# Cache Configuration
CACHE_DRIVER=file

# Session Configuration
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Sanctum Configuration
SANCTUM_STATEFUL_DOMAINS=localhost:8000
SESSION_DOMAIN=localhost

# Instructions:
# 1. Copy this content to a new file named .env
# 2. Update the database credentials for your PostgreSQL setup
# 3. Configure your email settings (Gmail, Mailtrap, or other SMTP provider)
# 4. Run: php artisan key:generate
# 5. Run: php artisan migrate
# 6. Run: php artisan db:seed
